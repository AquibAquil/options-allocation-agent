# Alpaca paper trading. Never commit real values; .env is gitignored.
# These names are what the official Alpaca MCP server reads, and alloc_agent
# reads the same ones, so this single file drives both.
ALPACA_API_KEY=
ALPACA_SECRET_KEY=
ALPACA_PAPER_TRADE=true

# Allocator / Challenger
ANTHROPIC_API_KEY=
